# 메이플랜드 버프 알람 — 설계 문서

> 상태: **M1 + M2 구현 완료** — 실행 방법과 사용법은 [`README.md`](README.md) 참고
> 최종 수정: 2026-08-16

---

## 1. 개요

메이플랜드 플레이 중 사용하는 버프(스킬 버프 / 소환수 / 소모품)의 지속시간을 추적해서,
만료되기 전에 **음성과 오버레이로 다시 쓰라고 알려주는** 도구.

### 목표

- 버프가 끊긴 걸 뒤늦게 알아채는 상황을 없앤다
- 게임 화면에서 눈을 떼지 않고도 어떤 버프를 다시 써야 하는지 알 수 있다
- 사용자가 타이머를 관리하는 데 드는 조작을 0에 가깝게 만든다

### 비목표 (명시적으로 하지 않는 것)

- **게임에 키 입력을 주입하는 자동 재버프** — 매크로에 해당하며 제재 대상. 절대 구현하지 않는다.
- 게임 메모리 읽기 / 프로세스 인젝션 — 안티치트 충돌 및 제재 위험.
- 캐릭터 스펙 계산기, 사냥 효율 계산 등 부가 기능 (범위 밖).

이 도구가 지키는 선은 **"읽기만 하고 알려주기만 한다"** 입니다.
키보드 입력을 엿듣는 것과 화면에 창을 띄우는 것까지가 전부이고,
게임 프로세스에 어떤 것도 쓰지 않습니다.

---

## 2. 플랫폼 결정: Electron 데스크톱 앱

### 결정

**Windows 데스크톱 앱 (Electron)** 으로 만든다. 항상 위 표시(always-on-top) 오버레이 창 + 전역 키보드 후킹.

### 근거

메이플랜드는 MapleStory Worlds 클라이언트로 돌아가는 **PC 게임**이고,
플레이 중에는 게임 창이 포커스를 독점합니다. 여기서 웹은 구조적으로 불리합니다.

| 요구사항 | 웹 (브라우저) | 데스크톱 앱 |
|---|---|---|
| 게임 중 키 입력 감지 | ❌ 불가 (포커스 없으면 이벤트 안 옴) | ✅ 전역 후킹 |
| 백그라운드 타이머 정확도 | ⚠️ 탭 비활성 시 throttling | ✅ 정상 동작 |
| 게임 위에 오버레이 | ❌ 불가 | ✅ always-on-top |
| 소리/음성 재생 | ⚠️ 자동재생 정책 제약 | ✅ 자유 |
| 개발/배포 속도 | ✅ 빠름 | ⚠️ 빌드·서명 필요 |

웹의 유일한 장점인 "개발이 빠르다"는 초기 한정 이점이고,
핵심 가치인 **자동 감지**를 웹으로는 아예 만들 수 없으므로 데스크톱으로 확정합니다.

### 검토했지만 채택하지 않은 대안

- **웹 PWA** — 듀얼모니터에 띄워두고 수동 클릭하는 용도로는 충분. 다만 키 감지가 불가능해서
  "사용자가 매번 클릭해야 하는 도구"가 되고, 그러면 결국 안 쓰게 됩니다. 보조 수단으로만 고려.
- **모바일 앱** — 휴대폰을 계속 쳐다봐야 하고 PC와 동기화 문제가 생김. 알림 채널로서의
  가치는 있으나(2차), 주 플랫폼으로는 부적합.
- **Tauri** — 번들 용량(약 5MB vs Electron 약 80MB)과 메모리에서 유리하지만,
  전역 키 후킹을 Rust(`rdev`)로 다뤄야 함. 웹 기술만으로 완결되는 Electron 쪽이
  초기 개발 마찰이 훨씬 적어 1차 버전은 Electron으로 간다. 완성 후 이식은 선택지로 남겨둠.
- **AutoHotkey** — 가장 빠르게 만들 수 있지만 UI/음성/설정 관리가 빈약하고,
  게임 관련 AHK 스크립트는 안티치트에서 오탐될 여지가 있어 제외.

---

## 3. 버프 감지 방식

### 결정

**전역 키보드 후킹**을 주 방식으로 하고, **수동 클릭**을 항상 병행 가능하게 둔다.

사용자가 게임에서 이프리트를 쓰려고 F3을 누르면, 앱이 그 키 이벤트를 받아서
이프리트 타이머를 자동으로 시작합니다. 사용자는 평소대로 게임만 하면 됩니다.

### 방식 비교

| 방식 | 정확도 | 난이도 | 판단 |
|---|---|---|---|
| 수동 클릭 / 앱 단축키 | 사용자 실수 시 어긋남 | 낮음 | **1차 구현** (폴백으로 영구 유지) |
| 전역 키 후킹 | 높음 | 중간 | **2차 구현** (핵심 기능) |
| 화면 캡처 + 버프 아이콘 인식 | 이론상 최고 | 높음 | 보류 |

### 화면 인식을 보류하는 이유

해상도·UI 스케일·버프바 위치에 따라 전부 깨지고, 게임 업데이트마다 유지보수가 필요합니다.
게다가 **이프리트 같은 소환수는 버프바에 아이콘이 뜨지 않아** 정작 가장 중요한 대상을 못 잡습니다.
비용 대비 실익이 낮아 로드맵 후반의 선택 기능으로만 남깁니다.

### 키 후킹의 알려진 한계 (설계에 반영 필요)

1. **쿨타임/마나부족/스턴으로 시전이 실패해도** 키는 눌렸으므로 타이머가 시작됩니다.
   → 완화: 같은 버프의 재입력을 짧은 시간 내 무시(디바운스), 그리고 언제든 수동 취소 가능.
2. **사망하면 대부분의 버프가 해제**되지만 앱은 모릅니다.
   → 완화: "전체 리셋" 전역 단축키(예: `Ctrl+Alt+R`)를 제공.
3. **소환수는 맵 이동·채널 이동·사망 시 사라집니다.**
   → 완화: 소환수 계열 버프에 `volatile: true` 플래그를 두고, 전체 리셋 대상에 우선 포함.
4. 게임이 관리자 권한으로 실행 중이면 후킹이 안 될 수 있음 → 앱도 관리자 권한 실행 안내.

### 단축키 범위와 후킹 설계

메이플랜드는 알파벳, `Ins` `Home` `PgUp` `Del` `End` `PgDn`, 숫자키, `Shift` `Ctrl` `Alt` 까지
거의 모든 키를 스킬 슬롯에 배치할 수 있습니다. 이 사실이 구현 방식을 하나 확정시킵니다.

#### Electron `globalShortcut`을 쓰지 않는다 (중요)

`globalShortcut.register()`는 키를 **등록해서 가로챕니다.** `PgUp`을 등록하면 그 키 이벤트가
게임에 전달되지 않아 **스킬이 안 나갑니다.** 버프 감지에는 반드시 `uiohook-napi`의
**수동적(passive) 리스닝**만 사용합니다 — 앱은 키 이벤트를 관찰만 하고 소비하지 않습니다.

이는 앱 자체 단축키(전체 리셋 / 음소거 / 프로필 전환)에도 동일하게 적용됩니다.
사용자가 사실상 모든 키를 게임에서 쓰므로, 앱 단축키를 `globalShortcut`으로 잡으면
게임 입력을 훔칠 위험이 있습니다. → **앱 단축키도 같은 uiohook 스트림에서 판정**합니다.

#### 키 등록 UX

문자열로 타이핑하게 하지 않습니다. **"키 입력 받기" 버튼 → 다음에 누르는 키를 그대로 캡처**해서
keycode로 저장합니다. 이렇게 하면 `Ins`/`PgUp` 같은 키나 **상단 숫자열 vs 넘패드** 구분을
사용자가 신경 쓸 필요가 없어집니다 (물리 keycode가 다르므로 자동으로 구분됨).

#### 수식키 단독 배치 처리

`Shift` 하나만 스킬 키로 쓰는 경우를 `Shift+A` 조합과 구분해야 합니다.
`Shift+A`를 누를 때도 Shift의 keydown은 발생하기 때문입니다.

→ **수식키는 keyup 시점에, 눌려 있는 동안 다른 키가 눌리지 않았을 때만** 발동으로 판정합니다.

```
Shift↓ ......... Shift↑          → 수식키 단독 발동 ✅
Shift↓ ... A↓ A↑ ... Shift↑      → Shift+A 조합, Shift 단독은 발동 안 함 ❌
A↓ ... Shift↓ Shift↑ ... A↑      → A 를 누르고 있었으므로 Shift 단독 아님 ❌
Ctrl↓ Shift↓ Shift↑ Ctrl↑        → 어느 쪽도 단독 아님 ❌
```

구현할 때 세 번째 줄을 놓치기 쉽습니다. "누르고 있는 동안 다른 키가 눌렸는가"만 보면
**이미 눌려 있던** 키를 놓칩니다. 이동키를 누른 채 Shift를 건드리는 건 사냥 중 흔한 일이라,
현재 눌려 있는 키 집합을 따로 들고 판정해야 합니다.

#### 그 외

- **키 반복 무시**: 키를 누르고 있으면 OS가 반복 이벤트를 보냄 → 같은 키 재입력은 300ms 무시.
  조합마다 따로 세므로 다른 키는 막히지 않는다.
- **좌/우 수식키**: 물리 keycode 를 쓰므로 자연히 구분된다 (좌 Shift 42, 우 Shift 54).
  게임에서 좌우를 다른 슬롯에 배치했을 수 있으므로 구분하는 쪽이 맞다.
- **충돌 검사**: 같은 키에 두 개 이상이 걸리면 설정 화면에 경고를 띄운다.
  검사 대상은 항상 살아 있는 것(앱 단축키 · 캐릭터 전환 키)과
  활성 캐릭터의 버프 키다. 비활성 캐릭터의 버프 키는 매핑에 없으므로 충돌하지 않는다.
- **Esc 는 등록할 수 없다**: 캡처 취소에 쓰기 때문.

---

## 4. 알림 정책

### 음성(TTS)을 1순위로

게임 화면을 보고 있는 상태에서는 오버레이의 숫자를 읽지 않습니다.
"이프리트", "소울애로우" 처럼 **버프 이름을 말해주는 음성**이 압도적으로 실용적입니다.

- 구현: Web Speech API (`speechSynthesis`) — Electron 렌더러에서 바로 사용 가능, 추가 의존성 없음
- **음성 선택**: OS에 설치된 음성 목록에서 고를 수 있고, 음량·속도·높낮이를 조절한다.
  기본값은 자동(한국어 음성 우선)이며, 고른 음성이 사라지면 자동으로 되돌아간다.
  음성 목록은 렌더러만 알 수 있으므로 오버레이 렌더러가 메인으로 보고하고,
  메인이 설정 창에 전달한다.
- 버프별로 읽어줄 문구를 사용자가 지정 가능 (`tts` 필드). 짧을수록 좋음.
- 보조: 버프별 구분 가능한 효과음 + 오버레이 게이지 색 변화(초록 → 노랑 → 빨강)

### 사전 알림

만료 **직전이 아니라 N초 전**에 알립니다. 기본 10초, 버프별로 조정 가능.
사냥 사이클을 끊지 않고 자연스러운 타이밍에 재버프할 수 있게 하는 게 목적입니다.

### 묶음 알림 (중요)

버프를 6~8개씩 쓰면 알림이 계속 울려서 결국 앱을 끄게 됩니다. 이를 막기 위해:

- **합류 윈도우**: 만료 예정 시각이 서로 30초 이내인 버프들은 하나로 묶어
  `"리버프"` 또는 `"이프리트, 소울애로우"` 처럼 한 번만 발화
- **최소 알림 간격**: 어떤 경우에도 N초(기본 5초) 안에 두 번 울리지 않음
- **조용 모드**: 전역 단축키로 음성만 즉시 음소거 (타이머는 계속 동작)

### 알림 상태 머신

각 버프는 사이클마다 아래 상태를 한 번씩만 통과하며, 각 전이에서 알림은 **1회만** 발화됩니다.

```
IDLE ──(시전 감지)──> ACTIVE ──(남은시간 ≤ preNotice)──> SOON ──(남은시간 ≤ 0)──> EXPIRED
  ^                                                                                  │
  └──────────────────────(재시전 / 수동 리셋)──────────────────────────────────────┘
```

---

## 5. 데이터 모델

### 핵심 결정: 캐릭터 단위, 전역 카탈로그 없음

설정 단위는 **캐릭터**입니다. 직업 단위로 잡으면 무너집니다.

- 같은 직업이어도 캐릭터마다 **스킬 레벨이 달라 지속시간이 다릅니다**
- 캐릭터마다 단축키 배치를 다르게 해뒀을 수 있습니다
- 같은 직업의 부캐를 여럿 키우면 "궁수" 하나로는 표현이 안 됩니다

그리고 **버프 목록과 지속시간은 사용자가 캐릭터별로 직접 입력**합니다.
초기 설계에는 "직업별 버프 후보를 미리 제안"하기 위한 전역 카탈로그 계층이 있었으나,
사용자가 직접 입력하는 이상 그 계층이 하는 일이 없어 **제거했습니다.**
`BuffDef` + `BuffBinding` 두 타입은 `Buff` 하나로 합쳐집니다.

이 결정의 부수 효과:

- 데이터 모델이 **Profile → Buff** 2계층으로 단순해짐 (참조 무결성 관리 불필요)
- 직업별 버프 시드 데이터를 조사·유지보수할 필요가 사라짐 —
  어차피 스킬 레벨에 따라 값이 달라서 정확한 기본값을 넣기 어려웠던 부분
- 대신 **프로필 복제**가 새 캐릭터 등록의 주 경로가 됨 (아래 참조)

### Hotkey

```ts
interface Hotkey {
  keycode: number;         // uiohook 물리 keycode (넘패드/숫자열 자동 구분)
  ctrl: boolean;
  alt: boolean;
  shift: boolean;
  bareModifier: boolean;   // 수식키 단독 배치 (keyup 판정 대상)
  label: string;           // 표시용. "PgUp", "Shift+A"
}
```

### Buff (캐릭터에 속함)

```ts
interface Buff {
  id: string;              // 내부 식별자 (자동 생성)
  name: string;            // "이프리트" — 사용자가 직접 입력
  durationSec: number;     // 이 캐릭터 스킬 레벨 기준 — 사용자가 직접 입력
  hotkey: Hotkey | null;   // 게임 내 단축키. "키 입력 받기"로 캡처
  preNoticeSec: number;    // 사전 알림 (기본 10)
  tts?: string;            // 미지정 시 name 을 그대로 읽음
  volatile: boolean;       // 맵이동/사망 시 사라지는가 (소환수 체크박스)
  enabled: boolean;
}
```

`category` 필드는 제거했습니다. 소모품 개수 카운터가 범위에서 빠지면서
분류가 어떤 동작에도 관여하지 않게 되었기 때문입니다.
스킬이든 소환수든 소모품이든 앱 입장에서는 "이름 + 지속시간 + 키"로 동일합니다.

### Profile (캐릭터)

```ts
interface Profile {
  id: string;
  name: string;            // 캐릭터 이름
  job?: string;            // 선택. 목록에서 구분하기 위한 라벨일 뿐, 로직 없음
  switchHotkey: Hotkey | null; // 게임 중 이 캐릭터로 전환. 복제 시 물려받지 않는다
  buffs: Buff[];
  overlayPos?: { x: number; y: number };  // 캐릭터별 오버레이 위치 (선택)
}
```

### BuffState (런타임 — 저장하지 않음)

```ts
interface BuffState {
  buffId: string;
  endsAt: number | null;   // epoch ms. null이면 비활성
  phase: "IDLE" | "ACTIVE" | "SOON" | "EXPIRED";
}
```

### 입력 부담을 줄이는 두 가지 (계층 추가 없이)

전역 카탈로그가 없으므로 새 캐릭터는 빈 목록에서 시작합니다. 이를 보완합니다.

1. **프로필 복제** — 새 캐릭터 등록의 주 경로. 비슷한 캐릭터를 복제한 뒤
   지속시간과 단축키만 수정하면 됩니다.
2. **버프 이름 자동완성** — 입력창에서 *다른 프로필에 이미 입력된 버프 이름*을 제안합니다.
   `이프리트` / `이프리트 `(뒤에 공백) 처럼 오타로 갈리는 걸 막습니다.
   저장 구조는 그대로이고 **UI 편의 기능일 뿐**입니다 — 공유 계층이 다시 생기는 게 아닙니다.

### Settings

```ts
interface Settings {
  activeProfileId: string | null;
  ttsEnabled: boolean;
  ttsVoiceURI: string | null; // null = 자동 (한국어 음성 우선)
  ttsVolume: number;        // 0.0 ~ 1.0
  ttsRate: number;          // 발화 속도
  ttsPitch: number;         // 음 높낮이
  soundEnabled: boolean;
  groupWindowSec: number;   // 묶음 알림 합류 윈도우 (기본 30)
  minAlertGapSec: number;   // 최소 알림 간격 (기본 5)
  hotkeyHookEnabled: boolean;
  appHotkeys: {             // uiohook으로 판정 (globalShortcut 사용 안 함)
    // 기본값은 전부 null. 게임에서 거의 모든 키를 쓰므로 미리 정해두면 충돌한다.
    resetAll: Hotkey | null;
    resetVolatile: Hotkey | null;
    muteToggle: Hotkey | null;
    toggleOverlay: Hotkey | null;
  };
  overlay: {
    mode: "list" | "compact";
    x: number; y: number;
    opacity: number; scale: number;
    clickThrough: boolean;
  };
}
```

저장은 `electron-store` (JSON 파일) 사용. 서버·DB·계정 불필요.
저장 파일 구조도 `{ profiles: [...], settings: {...} }` 두 갈래로 끝납니다.

### 프로필 전환 시 동작

1. **실행 중인 타이머 전부 클리어** — 캐릭터를 바꿨다는 건 이전 버프가 전부 무의미하다는 뜻
2. 오버레이 목록을 새 프로필의 활성 버프로 재구성
3. 키 후킹 매핑 테이블 교체 (keycode → buffId)
4. 오버레이 상단에 현재 프로필 이름 갱신

전환 수단은 세 가지: 트레이 메뉴 / 설정창 드롭다운 / **전역 단축키**.
게임 중 캐릭터를 바꿀 때 Alt+Tab 없이 전환해야 하므로 **단축키가 기본 경로**입니다.

전환 키는 추상적인 "슬롯 1~9"가 아니라 **캐릭터 자체(`Profile.switchHotkey`)에 붙입니다.**
슬롯 번호는 캐릭터를 추가·삭제할 때마다 의미가 밀리지만, 캐릭터에 직접 붙으면 그럴 일이 없습니다.
기본값은 비워 둡니다 — 게임에서 거의 모든 키를 쓰므로 미리 정한 조합은 충돌할 확률이 높습니다.

> 실행 중인 캐릭터를 **자동 인식**하려면 화면 인식(OCR)이 필요해서 보류합니다. 수동 전환으로 갑니다.

---

## 6. 버프 목록과 지속시간

### 지속시간을 하드코딩하지 않는 이유

메이플랜드의 버프 지속시간은 **스킬 레벨에 따라 달라집니다**.
(예: 소울 애로우는 스킬 레벨이 오를수록 지속시간이 늘어납니다.)
따라서 사람마다, 캐릭터마다 값이 다릅니다.

→ **버프 이름과 지속시간은 전부 사용자가 캐릭터별로 직접 입력**합니다.
앱은 기본 버프 목록을 동봉하지 않습니다.
같은 소울 애로우라도 본캐와 부캐의 스킬 레벨이 다르면 지속시간이 다르기 때문입니다.

> 사용자가 입력하는 건 항상 **"초"** 입니다. 뒤의 §9에서 다루는 "절대 시각 기반"은
> 앱 내부에서 오차 누적을 막기 위한 계산 방식일 뿐이고, 입력 화면과는 무관합니다.

### 초기 논의 대상이었던 버프

아래는 설계 논의의 출발점이 된 예시일 뿐, **앱에 내장되지 않습니다.**
`volatile` 처리 등 설계상 특이 케이스를 확인하기 위한 참고 목록입니다.

| 버프 | 성격 | 설계상 특이사항 |
|---|---|---|
| 이프리트 | 소환수 | 버프바에 아이콘 없음 · 맵이동/사망 시 소멸 → `volatile: true` |
| 메디테이션 | 스킬 | 파티원에게도 적용 |
| 포커스 | 스킬 | |
| 소울 애로우 | 스킬 | |
| 속도향상의 알약 | 소모품 | 지속시간 고정 (스킬 레벨 무관) |
| 아기용의 이유식 | 소모품 | 지속시간 고정 (스킬 레벨 무관) |

> 지속시간 수치는 이 문서에 넣지 않습니다. 메이플랜드 기준 레벨별 수치를 신뢰할 만한
> 형태로 확보하지 못했고([참고](https://www.inven.co.kr/board/maple/2816/1011)),
> 애초에 캐릭터마다 다르므로 **스킬창 실제 값을 보고 입력**하는 것이 맞습니다.

### 확장

부스터(무기별), 헤이스트, 샤프아이즈, 홀리심볼, 블레스, 하이퍼바디, 다른 소환수(엘퀴네스,
바하뮤트, 실버호크, 골든이글 등) 무엇이든 사용자가 이름만 적으면 그대로 동작합니다.
앱은 버프의 정체를 모르고, 알 필요도 없습니다 — **이름 + 지속시간 + 키**가 전부입니다.

이 무지함이 오히려 장점입니다. 게임에 새 버프가 추가되거나 밸런스 패치로 지속시간이
바뀌어도 앱을 업데이트할 필요가 없고, 문서화되지 않은 버프도 사용자가 바로 등록할 수 있습니다.

---

## 7. 화면 기획

### 전제: 노트북 단일 화면 + 게임 창모드

이 조건이 오버레이 설계를 규정합니다.

- ✅ **창모드이므로 always-on-top 오버레이가 정상 표시됩니다.** (전체화면 리스크 해소)
- ⚠️ 화면이 하나뿐이라 오버레이가 **게임 UI를 가리면 안 됩니다** — 미니맵, 퀵슬롯, HP/MP 바 회피
- ⚠️ 노트북 세로 해상도가 좁으므로 **세로로 긴 리스트는 부담**입니다
- ⚠️ 설정 창을 띄우려면 Alt+Tab이 필요하므로, **게임 중 조작은 전부 전역 단축키로** 가능해야 합니다

### 7.1 오버레이 창 (상시 표시)

게임 위에 떠 있는 작고 반투명한 창. 클릭은 뒤(게임)로 통과시킵니다.
표시 모드를 두 가지 제공합니다.

**리스트 모드** — 정보량 우선

```
┌──────────────────────────────┐
│ ◆ 주술사 부캐                 │  ← 현재 프로필 (전환 실수 방지)
├──────────────────────────────┤
│ 이프리트      ████████░░ 42초 │  ← 초록
│ 소울애로우    ███░░░░░░░ 12초 │  ← 노랑 (사전 알림 구간)
│ 메디테이션    █░░░░░░░░░  3초 │  ← 빨강 (깜빡임)
│ 이유식        ──────────  만료│  ← 회색
└──────────────────────────────┘
```

**컴팩트 모드** — 화면 점유 최소화 (노트북 기본값 후보)

```
┌────────────────────────────────────────────┐
│ 주술사 부캐 │ 이프리트 42 · 소울 12 · 메디 3 │
└────────────────────────────────────────────┘
```

가로 한 줄만 차지하므로 게임 화면 최상단이나 최하단에 붙여두기 좋습니다.
사전 알림 구간에 들어간 항목만 색이 바뀌므로 흘끗 봐도 구분됩니다.

- 남은 시간 짧은 순으로 정렬 (다음에 뭘 눌러야 하는지가 항상 맨 앞)
- 만료된 항목은 뒤로 밀거나 회색 처리
- 기본 위치는 게임 창 **우측 상단 미니맵 아래**를 제안, 드래그로 조정 후 저장
- `setIgnoreMouseEvents(true, { forward: true })` 로 클릭 통과
- 설정 창에서 "오버레이 잠금 해제"를 켰을 때만 드래그 가능 (게임 중 실수 방지)
- 오버레이 표시/숨김은 전역 단축키로 즉시 토글 (스크린샷 찍을 때 등)

> 화면이 하나라 오버레이가 어떻게든 게임 화면을 조금은 가립니다.
> 그래서 **TTS가 여전히 주 알림 채널**이고, 오버레이는 "지금 상태 확인용" 보조입니다.

### 7.2 메인/설정 창 (필요할 때만)

- **캐릭터 탭**: 프로필 목록 — 생성 / **복제** / 삭제 / 전환
  - 새 캐릭터는 빈 버프 목록에서 시작. 복제하면 기존 캐릭터의 버프 목록을 그대로 물려받음
  - 복제가 새 캐릭터 등록의 주 경로입니다 (전역 카탈로그가 없으므로)
- **버프 탭**: 선택된 프로필의 버프 표 — 이름 / 지속시간(초) / 단축키 / 사전알림 / 소환수 / 활성화 / 즉시 시작
  - 이름 칸은 다른 프로필에 이미 입력된 이름을 자동완성으로 제안
  - 단축키 칸은 "키 입력 받기" 버튼 → 누른 키를 그대로 캡처
  - "소환수" 체크박스가 `volatile` — 맵이동/사망 시 사라지는 버프 표시
  - "즉시 시작" 버튼이 수동 모드의 폴백이자, 키 후킹 없이도 쓸 수 있는 경로
- **설정 탭**: 음성/소리, 묶음 알림 파라미터, 앱 단축키(+충돌 경고), 오버레이 외형·모드
- **트레이 아이콘**: 창을 닫아도 트레이에 상주. 우클릭으로 **프로필 전환** / 음소거 / 오버레이 토글 / 종료

---

## 8. 아키텍처

```
┌──────────────────── Electron Main Process ───────────────────┐
│  hotkey-hook.js    uiohook-napi 수동 리스닝 (키를 소비하지 않음)│
│                     → 버프 키 / 앱 키를 한 스트림에서 판정      │
│  profile.js        활성 프로필 관리 · 전환 시 매핑 테이블 교체  │
│  timer-core.js     버프 상태 머신 + 만료 판정 (단일 진실 원천)│
│  notifier.js       묶음 알림 · 최소 간격 · 발화 큐             │
│  store.js          electron-store: 프로필/설정 영속화          │
│  windows.js        오버레이 창 · 설정 창 · 트레이 관리         │
└───────────────┬──────────────────────────┬───────────────────┘
                │ IPC (상태 브로드캐스트)   │ IPC (사용자 조작)
      ┌─────────▼─────────┐      ┌─────────▼──────────┐
      │  Overlay Renderer │      │ Settings Renderer  │
      │  게이지 렌더링     │      │ 버프/설정 편집      │
      │  TTS · 효과음 재생 │      └────────────────────┘
      └───────────────────┘
```

**설계 원칙: 타이머의 진실은 메인 프로세스에만 둡니다.**
렌더러 창은 닫히거나 다시 뜰 수 있으므로 상태를 들고 있으면 안 됩니다.
렌더러는 메인이 브로드캐스트하는 상태를 그리기만 합니다.

---

## 9. 타이머 코어 로직

### 절대 시각 기반 (필수)

> **사용자는 시각을 입력하지 않습니다.** 입력하는 값은 언제나 `"이프리트 = 220초"` 처럼
> **초 단위 지속시간**입니다. 아래는 그 초를 앱이 내부에서 어떻게 다루는지에 대한 이야기이고,
> 화면에 드러나지 않습니다.
>
> ```
> 키 감지 시각 14:30:00  +  220초  →  endsAt = 14:33:40 을 저장
> 매 틱마다: endsAt − 현재시각 = 남은 시간
> ```

`남은시간 -= 1` 같은 카운트다운 누산 방식은 반드시 어긋납니다.
`setInterval`은 정확히 그 주기에 안 돌고, 오차가 계속 쌓입니다.
**종료 시각을 저장하고 매 틱마다 현재 시각과 비교**해야 합니다.

```js
// 시전 감지 시
state.endsAt = Date.now() + buff.durationSec * 1000;
state.phase  = "ACTIVE";

// 틱 루프 (200ms 주기)
function tick() {
  const now = Date.now();
  const due = [];

  for (const [id, st] of states) {
    if (st.endsAt == null) continue;
    const remainMs = st.endsAt - now;

    if (remainMs <= 0 && st.phase !== "EXPIRED") {
      st.phase = "EXPIRED";
      due.push({ id, kind: "expired" });
    } else if (remainMs <= buffs[id].preNoticeSec * 1000 && st.phase === "ACTIVE") {
      st.phase = "SOON";
      due.push({ id, kind: "soon" });
    }
  }

  if (due.length) notifier.enqueue(due, now);  // 묶음/쿨다운은 notifier가 처리
  broadcastToRenderers(states);
}
```

- 틱 주기 200ms — 사람이 인지하기에 충분히 매끄럽고 노트북 CPU 부담도 없음
- 상태 전이 시에만 알림을 큐에 넣으므로 중복 발화가 구조적으로 불가능
- 노트북 절전/최대절전에서 복귀해도 `Date.now()` 비교라 자동으로 정상 복구
  (누산 방식이었다면 절전 시간만큼 통째로 밀림)

### 묶음 알림 처리

```
notifier.enqueue(due, now):
  1. due 항목을 pending 버퍼에 추가
  2. groupWindowSec 안에 만료 예정인 다른 버프가 있으면 그것도 함께 끌어옴
  3. 마지막 발화로부터 minAlertGapSec 미만이면 발화를 지연
  4. 발화: 1개면 버프 이름, 2개 이상이면 "리버프" 또는 이름 나열
```

---

## 10. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 런타임 | Electron | Node 생태계 + 웹 UI |
| UI | 바닐라 JS + HTML/CSS | 오버레이는 요소 몇 개뿐. 프레임워크 불필요 |
| 전역 키 후킹 | `uiohook-napi` | 활발히 유지보수됨. `iohook`은 사망 상태라 사용 금지 |
| 설정 저장 | `electron-store` | JSON 파일 |
| 음성 | Web Speech API | 브라우저 내장, 의존성 0 |
| 빌드 | `electron-builder` | Windows portable exe 우선 (설치 없이 실행) |

의존성은 의도적으로 최소화합니다. 이 앱은 서버도, 계정도, 네트워크 통신도 필요 없습니다.
**완전 오프라인 로컬 앱**입니다.

---

## 11. 로드맵

### M1 — 수동 모드 MVP ✅ 완료
- Electron 셸 + 오버레이 창(리스트/컴팩트) + 설정 창
- **Profile / Buff 데이터 모델** — 지속시간이 캐릭터에 붙으므로 M1부터 필요
- 프로필 CRUD + 복제, 버프 CRUD (이름·지속시간 직접 입력), 이름 자동완성
- 버튼 클릭으로 타이머 시작 → 게이지 + TTS 알림
- **이 시점에서 이미 실사용 가능** (창모드 전제)

### M2 — 자동 감지 (핵심) ✅ 완료
- `uiohook-napi` 수동 리스닝 → keycode 매핑 → 자동 타이머 시작
- "키 입력 받기" 캡처 UI, 수식키 단독 keyup 판정, 키 반복 디바운스
- 앱 단축키(전체 리셋 / 음소거 / 오버레이 토글 / 프로필 전환)를 같은 스트림에서 처리
- 단축키 충돌 검사, `volatile` 처리

### M3 — 실사용 다듬기
- 묶음 알림 · 최소 알림 간격
- 트레이 상주 + 트레이에서 프로필 전환
- 설정 내보내기/가져오기 (JSON) — 노트북을 바꾸거나 초기화해도 재입력 불필요
- portable exe 배포

### M4 — 선택 기능
- 화면 캡처 기반 버프 상태 검증 / 캐릭터 자동 인식
- 소모품 개수 카운터 (현재 범위에서 제외)
- 휴대폰 알림 연동

---

## 12. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **자동 입력 오해** | 계정 제재 | 입력 주입 코드를 아예 넣지 않음. README에 명시 |
| **`globalShortcut`이 게임 키를 가로챔** | 스킬이 안 나감 | 버프·앱 단축키 모두 uiohook 수동 리스닝으로만 처리 |
| 안티치트가 키 후킹을 오탐 | 앱 동작 불가 | 후킹 끄고 수동 모드로 쓸 수 있는 경로를 항상 유지 |
| 게임이 관리자 권한 실행 | 키 이벤트 수신 실패 | 앱도 관리자 권한 실행 안내 |
| 시전 실패 시 타이머 오작동 | 잘못된 알림 | 디바운스 + 수동 취소/리셋 |
| 알림 과다로 앱을 끄게 됨 | 도구 자체가 무용 | 묶음 알림 · 최소 간격을 M3가 아닌 초기부터 염두 |
| 프로필 전환을 잊고 사냥 | 엉뚱한 버프 알림 | 오버레이 상단에 현재 프로필 이름 상시 표시 |
| 노트북 단일 화면 점유 | 게임 UI 가림 | 컴팩트 모드 제공 + TTS를 주 채널로 |
| 노트북 성능 여유 부족 | 게임 프레임 저하 | 틱 200ms · DOM 최소 · 게이지는 CSS transform만 사용 |

> **전체화면 오버레이 문제는 해소됨** — 게임을 창모드로 운용하는 것이 확정되어
> always-on-top 오버레이가 정상 표시됩니다.

---

## 13. 미결정 사항

### 확정된 것

| 항목 | 결정 |
|---|---|
| 모니터 구성 | 노트북 단일 화면, 게임 **창모드** → 오버레이 정상 동작, 컴팩트 모드 제공 |
| 단축키 범위 | 알파벳 · `Ins`/`Home`/`PgUp`/`Del`/`End`/`PgDn` · 숫자키 · `Shift`/`Ctrl`/`Alt` 전부 |
| 프로필 단위 | 직업이 아니라 **캐릭터** (여러 직업 캐릭터 동시 운용) |
| 버프 목록·지속시간 | **사용자가 캐릭터별로 직접 입력.** 앱에 기본 목록 내장 안 함 |
| 전역 버프 카탈로그 | **없음** — Profile → Buff 2계층으로 단순화 |
| 소모품 개수 카운터 | **범위에서 제외** (M4로 이동) |

### 남은 것

구현 착수를 막는 항목은 없습니다. 아래는 **앱을 설치한 뒤 화면에서 채우는** 값들입니다.

- 캐릭터 프로필 등록 (이름, 필요하면 직업 라벨)
- 캐릭터별 버프 이름과 지속시간 — 스킬창을 보면서 입력
- 캐릭터별 단축키 — "키 입력 받기" 버튼으로 실제 키를 눌러 캡처
- 프로필 전환 단축키 기본값(`Ctrl+Alt+1`~`9`)이 게임 단축키와 겹치는지 확인 후 조정

즉 **M1 구현은 지금 바로 시작할 수 있습니다.** 앱이 특정 직업이나 버프를 미리 알 필요가
없어졌기 때문에, 캐릭터 구성이 정해지지 않아도 코드가 달라지지 않습니다.
